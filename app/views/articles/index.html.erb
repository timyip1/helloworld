<h1>Practice Logs</h1>

This file is app>views>articles>index.html.erb. 

<p>
 
<table>
  <tr>
    <th>How long?</th>
    <th>How did it go? </th>
    <th>Created</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= article.created_at %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<p>

<%= link_to 'Make a new entry', new_article_path(@article)%>